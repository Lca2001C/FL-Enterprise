# Dependências declaradas em pyproject.toml (fonte de verdade).
# Instalação recomendada: python -m pip install -e .
#
# Lock fechado (grafo transitivo resolvido no seu ambiente):
#   python -m pip install pip-tools
#   pip-compile pyproject.toml -o requirements-lock.txt
#
# Ou, após instalar o projeto em um venv limpo:
#   pip freeze > requirements-lock.txt
