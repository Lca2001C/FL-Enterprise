# Lock fechado (grafo transitivo). Regenerar após mudar pyproject.toml:
#   pip install pip-tools
#   pip-compile pyproject.toml -o requirements-lock.txt --strip-extras
#
# Instalação reproduzível (sem editable):
#   pip install -r requirements-lock.txt
#
# Fonte de verdade das dependências diretas: pyproject.toml
